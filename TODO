1. Add pop-up on launch to notify the user on frontend-keybind systems that the DS must be in focus for the keybinds to work
2. Add second window for stdout but bigger
3. Add keybinds in the frontend for !Linux/X11 systems
3a. Add backend handler for Windows, decide whether macOS is worth it
